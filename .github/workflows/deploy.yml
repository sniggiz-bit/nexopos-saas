name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  changes:
    name: Detect changed services
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      web: ${{ steps.filter.outputs.web }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'apps/api/**'
              - 'packages/shared/**'
            web:
              - 'apps/web/**'
              - 'packages/shared/**'

  deploy-api:
    name: Deploy API
    needs: changes
    if: needs.changes.outputs.api == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Build and deploy API on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          command_timeout: 30m
          script: |
            set -e
            cd /home/nexopos/nexopos.cl
            git fetch origin
            git checkout main
            git pull origin main
            docker build --no-cache --network=host -t nexoposcl-api:latest -f apps/api/Dockerfile.prod .
            docker-compose -f docker-compose.prod.yml up -d --no-build --force-recreate api
            echo "✓ API deployed"

  deploy-web:
    name: Deploy Web
    needs: changes
    if: needs.changes.outputs.web == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Build and deploy Web on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          command_timeout: 90m
          script: |
            set -e
            cd /home/nexopos/nexopos.cl
            git fetch origin
            git checkout main
            git pull origin main
            docker build --no-cache --network=host -t nexoposcl-web:latest -f apps/web/Dockerfile.prod .
            docker-compose -f docker-compose.prod.yml up -d --no-build --force-recreate web
            echo "✓ Web deployed"
